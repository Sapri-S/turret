# PROGRESS.md — Checklist

> Work the **next unchecked item**. Update this file as you go.
> `[ ]` todo · `[~]` in progress · `[x]` done · ⚠️ needs a user or advisor decision.

> **Status: planning. No code written yet.** Part 2 step 1 (project skeleton) is the
> starting point, and `src/` is deliberately empty until then.

## Done
- [x] Part 1 written — direction, hardware table, 12-step build, vision/data choices,
      safety boundaries, optional supervised accuracy trial, open decisions, 7 sources.
- [x] Part 2 written — "fast enough" targets, 4-stage threaded architecture,
      17 numbered software steps with "Done when" lines, test/metrics table, 8-week
      schedule, risks-and-fallbacks table.
- [x] Part 3 written — calibration, thermal/power, AI-HAT fallback, team/deliverables,
      git/backups, footage consent, licensing, spares, definition of done, and the six
      advisor questions.
- [x] Build script confirmed reproducible (2026-09-21) — its output matches the Sep 20
      `SkyStrike_V3.docx` snapshot exactly, so **no hand edits were ever lost**. The
      snapshot is kept at `docs/archive/SkyStrike_V3_2026-09-20.docx`.
- [x] Build script made path-relative (2026-09-21) — `ROOT` was hardcoded to the old
      Codex folder and would have written its output back there.
- [x] Moved into `turret/skystrike/` with agent context (2026-09-21).

## Next

### 1. Verify the plan renders correctly in Word ⚠️ needs you
- [x] Structure verified programmatically: 113 paragraphs, 11 tables, 26 headings,
      all three Parts present and in order.
- [ ] **Open it in Word and look at it.** Structure being right does not mean it
      *looks* right — check the **tables** and the **page breaks**. It was generated by
      script and has never been opened in Word.

### 2. Confirm the AI HAT variant ⚠️ blocks all installation
- [ ] Identify the exact HAT (AI Kit vs AI HAT+, and which TOPS rating). The runtime
      and the compiled model **must** match it.
- [ ] Do not install any runtime until this is settled.

### 3. Get advisor decisions ⚠️ advisor only — don't decide these
- [ ] **YOLOv8/YOLO11 are AGPL-3.0.** Needs a ruling before anything is built on them.
      Part 3 section C's classical HSV fallback is the license-clean alternative and
      doubles as an accuracy baseline.
- [ ] The six questions in Part 3 section I.
- [ ] Confirm the non-firing scope boundary in writing.

### 4. Fill in every blank
- [ ] Team roles (Part 3 section D).
- [ ] Deliverables and dates (Part 3 section D).
- [ ] Budget, and spares + lead time (Part 3 section G).
- [ ] Demo room.

### 5. Start the build
- [ ] Part 2 **step 1 — project skeleton** into `src/`. This is the first line of code.
- [ ] Then steps 2–17 in order, each to its own "Done when" line.
- [ ] Set up git + backups per Part 3 section E **before** there's code worth losing.

## Open questions
- Exact AI HAT variant? (blocks installation)
- AGPL-3.0 ruling on YOLO? (blocks the model choice)
- The six Part 3 section I advisor questions.
- Team roles, dates, budget, demo room.

## Log
- **2026-09-21** — Copied out of the Codex output folder into `turret/skystrike/`.
  Added `CLAUDE.md`/`AGENTS.md`, `PLAN.md`, this file, and `.context/`. Made the build
  script path-relative and confirmed it reproduces the Sep 20 snapshot exactly.
  **Naming note:** the handoff called the current plan
  `SkyStrike_Project_Plan_With_Software_Build.docx`, but no such file existed — it had
  been saved as `SkyStrike_V3.docx`. Both are the same content; the generated name is
  now canonical in `plan/` and the V3 snapshot is archived.
